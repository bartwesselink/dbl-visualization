export interface RotatedQuadOptions {
    x: number;
    y: number;
    width: number;
    height: number;
    rotation: number;

    color?: number[];
    fillColor?: number[];
    lineColor?: number[];
}