export interface CustomQuadOptions {
    x1: number;
    y1: number;

    x2: number;
    y2: number;

    x3: number;
    y3: number;

    x4: number;
    y4: number;

    color?: number[];
    fillColor?: number[];
    lineColor?: number[];

    // are not used but eases code
    x?: number;
    y?: number;
}
